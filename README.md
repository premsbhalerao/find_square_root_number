# find_square_root_number
Python Program to Find the Square Root

The python sqrt is an inbuilt function in programming language that can be found using 
the exponent’s operator and the cmath module.
